# Write a Python function that takes two integers as input and returns the greatest common divisor (GCD) of the two integers, 
# using the math module.

import math
a=int(input('enter a no: '))
b=int(input('enter another no: '))
s=(math.gcd(a,b))
print(s)